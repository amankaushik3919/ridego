name: Deploy — Nightly (12 AM)

on:
  schedule:
    - cron: '30 18 * * *'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy Rider Connect
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: curl -sS -X POST "${{ secrets.VERCEL_DEPLOY_URL }}"
